# `product_id: shakespeare`

Shakespeare — e.g. *Hamlet*, *Macbeth*, the sonnets.

Drop the Project Gutenberg `.txt` file(s) here (added in Phase 1 corpus prep). `product_id` is derived from this folder's name.
